# Ittan エージェントガイド

このリポジトリは、macOS用ファイルシェルアプリ Ittan のプロジェクトです。

## 憲法

**憲法はすべてのルールよりも上にあり、何よりも重要視される。**

このリポジトリで作業する前に `constitution/README.md` を読み、作業対象に関係する憲法へ従う。`AGENTS.md`、skill、設計書、既存実装が憲法と矛盾する場合は憲法を優先する。

## 最初に読むもの

1. まずこのファイルと `constitution/README.md` を読む。
2. コードを作成・変更・置換する前に `constitution/CODE_QUALITY.md` を読む。
3. Featureの作成・分割・命名を判断する前に `constitution/FEATURE_SLICED_DESIGN.md` を読む。
4. データベースを導入・変更する場合は `constitution/DATABASE.md` を読む。

## 作業ルール

- 変更範囲は依頼された領域に閉じる。
- secret、ビルド成果物、DerivedData、ローカル環境固有のファイルをコミットしない。
- 使用されなくなった実装は、置き換えた変更と同時に削除する。
- Xcodeプロジェクトとテストが成立する状態を維持する。

## 検証

- Swiftコードを変更した場合は、可能な限りIttanのテストを実行する。
- UIや起動動作を変更した場合は、Debug版をビルドして実際の起動を確認する。
- ドキュメントのみの変更ではビルド検証は不要とする。
